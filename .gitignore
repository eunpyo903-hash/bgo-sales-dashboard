__pycache__/
*.pyc
.env
credentials.json
token.json
.DS_Store

# 실제 매출 Excel 원본 파일은 저장소에 올리지 않는다 (data 폴더 구조만 유지)
data/*.xlsx
data/*.xlsm
!data/.gitkeep

# index.html 자동 백업본은 git 히스토리가 이미 대신하므로 버전관리하지 않는다
backup/*.html
!backup/.gitkeep
